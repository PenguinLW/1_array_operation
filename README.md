# 1_array_operation

Учебное Java-приложение для операций над векторами и матрицами с интерфейсом
Swing. В `src/com/mfi_2z` формы ввода отделены от классов с математическими
операциями. `app_build.sh` и `app_run.sh` предназначены для сборки и запуска.
